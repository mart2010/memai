This Python project is intended to develop an AI voice assistant that is solely mamaged by voice.  

It is structured as a monorepo including client and server implementation code (client and server respectively).
It must only depend on open-source libraries.
It must be able to run locally using no Cloud-based service.
The server side executes on a Linux workstation (Ubuntu) with GPU available.  
The client side executes on a Windows laptop.


