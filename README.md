This Python project is intended to develop an AI voice assistant that is solely managed by voice.  

It is structured as a monorepo including client and server implementation code.
It depends only on open-source libraries.
It must be able to run locally using no Cloud-based service.
The server side executes on a Linux workstation (Ubuntu) with GPU available.  
The client side executes on a Windows laptop.


